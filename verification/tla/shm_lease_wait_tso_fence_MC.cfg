\* THE REMEDY, under the same store-buffer abstraction: a SEQ-CST FENCE between
\* the value bump and the load of `waiters`, which forces the store to be visible
\* before the load -- the Dekker pairing `src/shm_lease/obsring.nim` ALREADY uses
\* for its structurally identical idle-token / `tail - head` pair.
\*
\* Everything HOLDS. This is what makes the finding next door actionable rather
\* than merely alarming: the model distinguishes the fix, and the fix is one the
\* same library already applies elsewhere.
SPECIFICATION Spec

CONSTANTS
    w1 = w1
    w2 = w2
    Waiters <- WaitersDef
    GrantWork <- GrantWorkDef
    MaxSpurious = 1
    PayloadFirst = TRUE
    AllowStoreLoadReorder = TRUE
    FenceAfterBump = TRUE
    OneOutstandingPerSlot = TRUE

INVARIANTS
    TypeOK
    NoLostWakeup
    WakesLEGrants
    WakeImpliesGrant
    GrantPayloadCoherent
    WaiterCountSane
    NoWakeWhileUnregistered

PROPERTIES
    AllServed
