\* MUTATION: `fitsPacked` replaced by a whole-word comparison. TLC MUST REPORT A
\* VIOLATION -- `NoBorrow` is expected to fail first, and `NoOvercommit` /
\* `ConservationExact` fail on the same behaviour.
SPECIFICATION Spec

CONSTANTS
    p1 = p1
    p2 = p2
    Procs <- ProcsDef
    Words = {0}
    Base = 4
    Dims = 2
    Capacity <- CapDef
    Want <- WantDef
    WordList <- ListDef
    Rounds = 1
    PerFieldFit = FALSE
    EnforceOrder = TRUE

INVARIANTS
    NoBorrow
    NoOvercommit
    ConservationExact
