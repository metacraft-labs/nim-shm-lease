\* NON-VACUITY PROBE -- REQUIRED TO FAIL, and it is the one that says this model
\* actually reaches the state it was built for.
\*
\* `NoDeadCombinerWithHead` says no behaviour ever has a combiner DEAD in the
\* middle of its round while that round's scan had named a RESERVATION HEAD. That
\* is precisely the interaction M6's `:deferred:` (4) says neither
\* `shm_lease_admit` nor `shm_lease_combine` can see, so it MUST be violated here
\* or the green run above proves nothing about it.
SPECIFICATION Spec

CONSTANTS
    s1 = s1
    s2 = s2
    big = big
    NoProc = none
    Smalls <- SmallsDef
    SlotSeq <- SlotSeqDef
    Large = big
    SmallWant = 1
    LargeWant = 3
    Capacity = 4
    MaxHold = 2
    MaxDeaths = 1
    Reserve = TRUE
    ArrivalOrder = TRUE
    Steal = TRUE
    Reclaim = TRUE
    ReclaimLive = FALSE

INVARIANT
    NoDeadCombinerWithHead
